<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.xmx.androidkotlinbase">

    <!-- SD卡读写权限 -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <!-- 震动权限 -->
    <uses-permission android:name="android.permission.VIBRATE" />
    <!-- LeanCloud基础模块 -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:fullBackupContent="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Splash.SplashActivity"
            android:theme="@style/AppTheme.SplashBackground">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Activities.TempActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Data.SQL.SQLActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Data.Cloud.CloudActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Data.Sync.SyncActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Log.ExceptionTestActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Log.OperationLogActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".User.LoginActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".User.RegisterActivity"
            android:theme="@style/AppTheme.NoActionBar" />

        <service
            android:name=".Services.MainService"
            android:enabled="true"
            android:exported="true" />
    </application>

</manifest>